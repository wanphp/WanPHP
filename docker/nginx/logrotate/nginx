/var/log/nginx/*.log {
    daily
    rotate 10
    missingok
    notifempty

    compress
    delaycompress
    dateext

    create 0644 www-data www-data
    sharedscripts

    postrotate
        [ -f /run/nginx.pid ] && kill -USR1 `cat /run/nginx.pid`
    endscript
}
